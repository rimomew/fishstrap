# PyFishstrap Requirements
# Python reimplementation of Fishstrap (https://github.com/rimomew/fishstrap)

# Core dependencies
aiohttp>=3.8.0
requests>=2.28.0

# UI dependencies
PyQt6>=6.4.0

# Windows-specific dependencies (for shortcuts, registry, etc.)
pywin32>=302 ; sys_platform == 'win32'

# Optional dependencies for better experience
# aiofiles>=22.1.0 ; for async file operations
# tqdm>=4.64.0 ; for progress bars

# Development dependencies
# pytest>=7.2.0
# pytest-asyncio>=0.20.0
# black>=22.10.0
# flake8>=5.0.0
# mypy>=0.990
